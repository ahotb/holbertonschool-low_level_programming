#include "main.h"
#include <stdio.h>

/**
* _strlen - one  pointer
* @s: pointer to an int
*
* Return: void
*/
int _strlen(char *s)
{
int i;
int c = 0;

for (i = 0; s[i] != '\0'; i++)
{
c++;
}
return (0);
}
