#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int un;
for (un = "0"; un <= "10"; un++)
putchar(un);
putchar('\n');
return (0);
}
